{
  "private": true,
  "scripts": {
    "esbuild": "esbuild --bundle --legal-comments=none --outdir=dist src/index.tsx",
    "build": "npm run esbuild -- --minify",
    "dev": "npm run esbuild -- --servedir=dist --serve-fallback=src/index.html --watch",
    "format": "prettier --write 'src/**/*.{css,html,ts,tsx}'"
  },
  "dependencies": {
    "@xtjs/lib": "^0.57.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "seedrandom": "^3.0.5"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@types/seedrandom": "^3.0.8",
    "esbuild": "^0.24.0",
    "prettier": "^3.3.3",
    "prettier-plugin-organize-imports": "^4.1.0",
    "typescript": "^5.6.3"
  }
}
